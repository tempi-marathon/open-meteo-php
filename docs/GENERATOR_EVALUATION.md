# Generator evaluation

Saloon SDK Generator dry-run against pinned OpenAPI YAML files under `openapi/` would produce an oversized surface area (hundreds of hourly variables). This package curates enums via `scripts/generate-hourly-daily-enums.php` and hand-written geocoding requests.
